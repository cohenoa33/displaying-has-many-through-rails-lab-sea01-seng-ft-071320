<h1> <%= @doctor.name %> </h1> 
<h2> <%= @doctor.department %> </h2> 
<ul>
<% @doctor.appointments.each do |app| %>
<li><%= app.appointment_datetime.strftime("%B %d, %Y") %> at <%= app.appointment_datetime.strftime("%k:%M")%> 
<%= link_to app.patient.name, app.patient %> </li>
<%end%>
</ul>


